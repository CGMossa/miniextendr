configure~
config.log
config.status
autom4te.cache
src/rust/.cargo/config.toml
src/Makevars
# Note: Makevars.win is a static file (uses `include Makevars`), tracked in git
src/*-wrappers.R

# Generated by the host cdylib pass on every `R CMD INSTALL`; untracked to
# avoid merge conflicts on these large generated files. They ship in the
# tarball from disk (.Rbuildignore does not exclude them).
R/*-wrappers.R
src/rust/wasm_registry.rs

# Built artifacts
src/*.o
src/*.so
src/*.dll

# Cargo target directories
# These MUST be outside src/ to avoid pkgbuild scanning generated .rs files
rust-target/
ra-target/
# Legacy locations (inside src/, causes pkgbuild issues)
src/rust/ra_target/
src/rust/target/

# Vendor tarball — generated at build/release time; never tracked.
inst/vendor.tar.xz
inst/.vendor.tar.xz.cksum

# Vendored crates (large, auto-generated by cargo vendor)
vendor/
