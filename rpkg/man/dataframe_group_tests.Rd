% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/miniextendr-wrappers.R
\name{group_by_keys}
\alias{group_by_keys}
\alias{group_by_sizes}
\alias{group_by_multi_keys}
\alias{group_by_multi_sizes}
\alias{group_by_multi_frames}
\alias{group_by_frames}
\alias{group_metadata_keys}
\alias{group_metadata_sizes}
\alias{group_metadata_frames}
\alias{group_by_extract_sums}
\alias{group_rows_summary}
\title{group_by_keys}
\source{
Generated by miniextendr from Rust fn \code{group_by_keys}

Generated by miniextendr from Rust fn \code{group_by_sizes}

Generated by miniextendr from Rust fn \code{group_by_multi_keys}

Generated by miniextendr from Rust fn \code{group_by_multi_sizes}

Generated by miniextendr from Rust fn \code{group_by_multi_frames}

Generated by miniextendr from Rust fn \code{group_by_frames}

Generated by miniextendr from Rust fn \code{group_metadata_keys}

Generated by miniextendr from Rust fn \code{group_metadata_sizes}

Generated by miniextendr from Rust fn \code{group_metadata_frames}

Generated by miniextendr from Rust fn \code{group_by_extract_sums}

Generated by miniextendr from Rust fn \code{group_rows_summary}
}
\usage{
group_by_keys(df, col)

group_by_sizes(df, col)

group_by_multi_keys(df, cols)

group_by_multi_sizes(df, cols)

group_by_multi_frames(df, cols)

group_by_frames(df, col)

group_metadata_keys(df)

group_metadata_sizes(df)

group_metadata_frames(df)

group_by_extract_sums(df)

group_rows_summary()
}
\arguments{
\item{df}{A data.frame with columns \code{g} (character) and \code{v} (double).}

\item{col}{The key column name.}

\item{cols}{The key column names.}
}
\description{
Group key labels in group order for any supported key column.

Group sizes in group order.

Composite group-key labels (\code{.}-joined) in group order, for multiple key columns. Mirrors \code{group_by_keys} for \code{DataFrame::group_by_multi}.

Composite-key group sizes in group order.

Named list of per-group sub-frames keyed by a composite \code{interaction()}-style key — the \code{split(df, interaction(...))} analogue (except NA-containing tuples form trailing groups instead of being dropped). Mirrors \code{group_by_frames}.

Named list of per-group sub-frames — the \code{split()} analogue (except NA keys form a trailing "NA" entry instead of being dropped).

Group-key labels in dplyr's group order, read from a \code{grouped_df}'s \code{groups} attribute (no recomputation). Multi-column keys are \code{.}-joined.

Per-group sizes in dplyr's group order, read from a \code{grouped_df}'s \code{groups} attribute. Empty (\code{.drop = FALSE}) groups appear as \code{0}.

Named list of per-group sub-frames materialised from a \code{grouped_df}'s \code{groups} metadata — exercises the 1-based→0-based \code{.rows} conversion by row content (each sub-frame holds exactly the caller's grouped rows).

One typed extraction + move-partition: per-group sum and count of \code{v}, grouped by \code{g}. Input schema: \code{g} (character, no NAs), \code{v} (double). Returns a data.frame with columns key/sum/n in group order.

Rung 1: rows synthesized internally, grouped Rust-side with \code{group_rows} (no SEXP contact in the grouping itself; \verb{Option<String>} keys give NA a home — \code{None} sorts first). Returns a key/sum/n summary frame.
}
