% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/miniextendr-wrappers.R
\name{memory_connection}
\alias{memory_connection}
\alias{string_input_connection}
\alias{counter_connection}
\alias{cursor_connection}
\alias{empty_cursor_connection}
\alias{uppercase_connection}
\alias{rot13_connection}
\alias{rust_get_stdout}
\alias{rust_get_stderr}
\alias{rust_write_to_stderr}
\alias{rust_get_null_connection}
\alias{rust_write_to_null}
\alias{rust_run_progress}
\alias{rust_run_progress_to_stderr}
\alias{rust_run_progress_explicit_close}
\title{memory_connection}
\source{
Generated by miniextendr from Rust fn \code{memory_connection}

Generated by miniextendr from Rust fn \code{string_input_connection}

Generated by miniextendr from Rust fn \code{counter_connection}

Generated by miniextendr from Rust fn \code{cursor_connection}

Generated by miniextendr from Rust fn \code{empty_cursor_connection}

Generated by miniextendr from Rust fn \code{uppercase_connection}

Generated by miniextendr from Rust fn \code{rot13_connection}

Generated by miniextendr from Rust fn \code{rust_get_stdout}

Generated by miniextendr from Rust fn \code{rust_get_stderr}

Generated by miniextendr from Rust fn \code{rust_write_to_stderr}

Generated by miniextendr from Rust fn \code{rust_get_null_connection}

Generated by miniextendr from Rust fn \code{rust_write_to_null}

Generated by miniextendr from Rust fn \code{rust_run_progress}

Generated by miniextendr from Rust fn \code{rust_run_progress_to_stderr}

Generated by miniextendr from Rust fn \code{rust_run_progress_explicit_close}
}
\usage{
memory_connection()

string_input_connection(content)

counter_connection(start, end)

cursor_connection(data)

empty_cursor_connection()

uppercase_connection(text)

rot13_connection(text)

rust_get_stdout()

rust_get_stderr()

rust_write_to_stderr(message)

rust_get_null_connection()

rust_write_to_null(message)

rust_run_progress(n)

rust_run_progress_to_stderr(n)

rust_run_progress_explicit_close(n)
}
\arguments{
\item{content}{Initial content for the buffer}

\item{start}{First integer to generate}

\item{end}{Last integer to generate (inclusive)}

\item{data}{Initial data for the cursor}

\item{text}{Input text to encode}

\item{message}{Character string to write (discarded by null device).}

\item{n}{Integer upper bound (bar goes from 0 to n inclusive).}
}
\value{
A custom R connection backed by an in-memory buffer

A custom R connection for reading the content

A read-only connection that generates one integer per line

A read-write-seekable connection backed by a Cursor

A write-seekable connection starting empty

A read-only connection that returns uppercase text

A read-only connection that returns ROT13-encoded text

R's stdout connection object.

R's stderr connection object.

An open write-only connection to the null device.

Logical \code{TRUE} if explicit close succeeded without error.
}
\description{
Create an empty in-memory connection for reading and writing.

Create a memory connection pre-filled with the given string.

Create a connection that generates sequential integers.

Create a connection using Rust's std::io Cursor.

Demonstrates the RConnectionIo adapter for wrapping std::io types.

Create an empty cursor connection for writing.

Create a connection that returns uppercase text.

Create a connection that ROT13-encodes text.

Return R's stdout() terminal connection from Rust.

Used in tests to verify the type round-trips correctly.

Return R's stderr() terminal connection from Rust.

Used in tests to verify the type round-trips correctly.

Write a message to R's stderr connection from Rust.

Useful for capturing stderr output from Rust code via \code{capture.output()}.

Open a null device connection from Rust and return it to R.

The no-arg signature makes this eligible for the gctorture GC sweep. The returned connection is open; R will close it when it is GC'd or when the caller calls \code{close()} on it.

Write a message to a new null connection from Rust (output discarded).

Exercises the full write path: allocate → write → auto-close on drop.

Run a style-3 progress bar from 0 to n on stdout.

Drives the full set/drop lifecycle; auto-closes when the function returns.

Run a style-3 progress bar from 0 to n, routing output to R's stderr.

Lets tests verify the \code{file} builder option works and that output appears on the message channel (where \code{sink(type="message")} can capture it).

Exercise the explicit \code{close()} path — Drop is a no-op after this.

Returns \code{TRUE} if \code{close()} succeeded. After \code{close()} consumes \code{self}, the \code{Drop} impl is skipped via \code{mem::forget}, so no double-close occurs.
}
\examples{
\dontrun{
conn <- memory_connection()
writeLines("Hello, World!", conn)
seek(conn, 0)
readLines(conn)  # "Hello, World!"
close(conn)
}
\dontrun{
conn <- string_input_connection("line1\nline2\nline3")
readLines(conn)  # c("line1", "line2", "line3")
close(conn)
}
\dontrun{
conn <- counter_connection(1L, 5L)
readLines(conn)  # c("1", "2", "3", "4", "5")
close(conn)
}
\dontrun{
conn <- cursor_connection(charToRaw("Hello"))
rawToChar(readBin(conn, "raw", 5))  # "Hello"
seek(conn, 0)
writeBin(charToRaw("Hi!!!"), conn)
seek(conn, 0)
rawToChar(readBin(conn, "raw", 5))  # "Hi!!!"
close(conn)
}
\dontrun{
conn <- empty_cursor_connection()
writeBin(as.raw(1:10), conn)
seek(conn, 0)
readBin(conn, "raw", 10)  # raw(10): 01 02 03 04 05 06 07 08 09 0a
close(conn)
}
\dontrun{
conn <- uppercase_connection("hello world")
readLines(conn)  # "HELLO WORLD"
close(conn)
}
\dontrun{
conn <- rot13_connection("hello")
readLines(conn)  # "uryyb"
close(conn)
# Applying ROT13 twice returns the original
conn2 <- rot13_connection("uryyb")
readLines(conn2)  # "hello"
}
}
