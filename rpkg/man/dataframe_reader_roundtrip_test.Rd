% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/miniextendr-wrappers.R
\name{reader_fixed_roundtrip}
\alias{reader_fixed_roundtrip}
\alias{reader_pinned_roundtrip}
\alias{reader_box_pinned_roundtrip}
\alias{reader_auto_roundtrip}
\alias{reader_auto_box_roundtrip}
\alias{reader_flatten_roundtrip}
\alias{reader_flatten_mixed_roundtrip}
\alias{reader_flatten_nested_roundtrip}
\alias{reader_list_vec_roundtrip}
\alias{reader_list_box_roundtrip}
\alias{reader_list_multi_roundtrip}
\alias{reader_list_vec_roundtrip_par}
\alias{reader_map_roundtrip}
\alias{reader_hashmap_roundtrip}
\alias{reader_map_roundtrip_par}
\alias{with_int_map_roundtrip}
\alias{with_int_map_roundtrip_par}
\alias{reader_fixed_roundtrip_par}
\alias{reader_flatten_roundtrip_par}
\title{reader_fixed_roundtrip}
\source{
Generated by miniextendr from Rust fn \code{reader_fixed_roundtrip}

Generated by miniextendr from Rust fn \code{reader_pinned_roundtrip}

Generated by miniextendr from Rust fn \code{reader_box_pinned_roundtrip}

Generated by miniextendr from Rust fn \code{reader_auto_roundtrip}

Generated by miniextendr from Rust fn \code{reader_auto_box_roundtrip}

Generated by miniextendr from Rust fn \code{reader_flatten_roundtrip}

Generated by miniextendr from Rust fn \code{reader_flatten_mixed_roundtrip}

Generated by miniextendr from Rust fn \code{reader_flatten_nested_roundtrip}

Generated by miniextendr from Rust fn \code{reader_list_vec_roundtrip}

Generated by miniextendr from Rust fn \code{reader_list_box_roundtrip}

Generated by miniextendr from Rust fn \code{reader_list_multi_roundtrip}

Generated by miniextendr from Rust fn \code{reader_list_vec_roundtrip_par}

Generated by miniextendr from Rust fn \code{reader_map_roundtrip}

Generated by miniextendr from Rust fn \code{reader_hashmap_roundtrip}

Generated by miniextendr from Rust fn \code{reader_map_roundtrip_par}

Generated by miniextendr from Rust fn \code{with_int_map_roundtrip}

Generated by miniextendr from Rust fn \code{with_int_map_roundtrip_par}

Generated by miniextendr from Rust fn \code{reader_fixed_roundtrip_par}

Generated by miniextendr from Rust fn \code{reader_flatten_roundtrip_par}
}
\usage{
reader_fixed_roundtrip(df)

reader_pinned_roundtrip(df)

reader_box_pinned_roundtrip(df)

reader_auto_roundtrip(df)

reader_auto_box_roundtrip(df)

reader_flatten_roundtrip(df)

reader_flatten_mixed_roundtrip(df)

reader_flatten_nested_roundtrip(df)

reader_list_vec_roundtrip(df)

reader_list_box_roundtrip(df)

reader_list_multi_roundtrip(df)

reader_list_vec_roundtrip_par(df)

reader_map_roundtrip(df)

reader_hashmap_roundtrip(df)

reader_map_roundtrip_par(df)

with_int_map_roundtrip(df)

with_int_map_roundtrip_par(df)

reader_fixed_roundtrip_par(df)

reader_flatten_roundtrip_par(df)
}
\arguments{
\item{df}{data.frame with \code{id}, \code{origin_x}, \code{origin_y}.}
}
\description{
\verb{Vec::<RFixedRow>::from_dataframe(&df)} → rebuild. Columns \code{id}, \code{coords_1..3}.

Pinned-width \verb{Vec<f64>} round-trip. Columns \code{name}, \code{scores_1..3}.

Pinned-width \verb{Box<[f64]>} round-trip. Columns \code{k}, \code{vals_1}, \code{vals_2}.

Auto-expand \verb{Vec<f64>} round-trip. Columns \code{name}, \code{values_1..values_k}.

Auto-expand \verb{Box<[i32]>} round-trip. Columns \code{tag}, \code{xs_1..xs_k}.

Struct-flatten round-trip. Columns \code{id}, \code{origin_x}, \code{origin_y}.

Struct-flatten with a \code{String} inner column. Columns \code{id}, \code{owner_label}, \code{owner_age}.

Recursive (three-level) struct-flatten round-trip. Columns \code{id}, \code{mid_a}, \code{mid_leaf_z}.

\verb{Vec::<RListVecRow>::from_dataframe(&df)} → rebuild. Columns \code{id}, \code{data} (list-column).

\verb{Vec::<RListBoxRow>::from_dataframe(&df)} → rebuild. Columns \code{tag}, \code{xs} (list-column). Exercises \code{.into()} from \verb{Vec<i32>} to \verb{Box<[i32]>} per row.

\verb{Vec::<RListMultiRow>::from_dataframe(&df)} → rebuild. Columns \code{ids}, \code{names} (both list-columns).

Parallel list-column round-trip (real off-thread index assembly). Columns \code{id}, \code{data}.

\verb{Vec::<RMapRow>::from_dataframe(&df)} → rebuild. Columns \code{id}, \code{opts} (list of named lists). BTreeMap keys come back sorted.

\verb{Vec::<RHashMapRow>::from_dataframe(&df)} → rebuild. Columns \code{label}, \code{weights} (list of named lists). HashMap key order is non-deterministic — R-side assertions must sort by name.

Parallel map-column round-trip (real off-thread index assembly). Columns \code{id}, \code{opts}.

\verb{Vec::<WithIntMap>::from_dataframe(&df)} → rebuild. Columns \code{id}, \code{tally_keys}, \code{tally_values} (VECSXP of integer/double vectors). BTreeMap keys come back sorted.

Parallel non-String-keyed map round-trip. Columns \code{id}, \code{tally_keys}, \code{tally_values}.

Parallel fixed-array round-trip (real off-thread assembly). Columns \code{id}, \code{coords_1..3}.

Parallel struct-flatten round-trip (delegates to sequential). Columns \code{id}, \code{origin_x}, \code{origin_y}.
}
