% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/miniextendr-wrappers.R
\name{re_scalar_roundtrip}
\alias{re_scalar_roundtrip}
\alias{re_scalar_align}
\alias{re_scalar_roundtrip_zero}
\alias{re_scalar_roundtrip_par}
\alias{re_expand_roundtrip}
\alias{re_expand_align}
\alias{re_expand_roundtrip_par}
\alias{re_move_roundtrip}
\alias{re_move_align}
\alias{re_tracked_roundtrip}
\alias{re_tracked_align}
\alias{re_tracked_roundtrip_par}
\alias{re_loc_roundtrip}
\alias{re_loc_align}
\alias{re_loc_roundtrip_par}
\alias{re_map_b_roundtrip}
\alias{re_map_b_align}
\alias{re_map_b_roundtrip_par}
\alias{re_map_h_roundtrip_ok}
\title{re_scalar_roundtrip}
\source{
Generated by miniextendr from Rust fn \code{re_scalar_roundtrip}

Generated by miniextendr from Rust fn \code{re_scalar_align}

Generated by miniextendr from Rust fn \code{re_scalar_roundtrip_zero}

Generated by miniextendr from Rust fn \code{re_scalar_roundtrip_par}

Generated by miniextendr from Rust fn \code{re_expand_roundtrip}

Generated by miniextendr from Rust fn \code{re_expand_align}

Generated by miniextendr from Rust fn \code{re_expand_roundtrip_par}

Generated by miniextendr from Rust fn \code{re_move_roundtrip}

Generated by miniextendr from Rust fn \code{re_move_align}

Generated by miniextendr from Rust fn \code{re_tracked_roundtrip}

Generated by miniextendr from Rust fn \code{re_tracked_align}

Generated by miniextendr from Rust fn \code{re_tracked_roundtrip_par}

Generated by miniextendr from Rust fn \code{re_loc_roundtrip}

Generated by miniextendr from Rust fn \code{re_loc_align}

Generated by miniextendr from Rust fn \code{re_loc_roundtrip_par}

Generated by miniextendr from Rust fn \code{re_map_b_roundtrip}

Generated by miniextendr from Rust fn \code{re_map_b_align}

Generated by miniextendr from Rust fn \code{re_map_b_roundtrip_par}

Generated by miniextendr from Rust fn \code{re_map_h_roundtrip_ok}
}
\usage{
re_scalar_roundtrip(df)

re_scalar_align()

re_scalar_roundtrip_zero(df)

re_scalar_roundtrip_par(df)

re_expand_roundtrip(df)

re_expand_align()

re_expand_roundtrip_par(df)

re_move_roundtrip(df)

re_move_align()

re_tracked_roundtrip(df)

re_tracked_align()

re_tracked_roundtrip_par(df)

re_loc_roundtrip(df)

re_loc_align()

re_loc_roundtrip_par(df)

re_map_b_roundtrip(df)

re_map_b_align()

re_map_b_roundtrip_par(df)

re_map_h_roundtrip_ok()
}
\arguments{
\item{df}{(no documentation available)}
}
\description{
Scalar tagged-union enum round-trip. Columns: \verb{_type}, \code{id}, \code{x}, \code{name}.

Produce the writer output for a mixed REScalar frame (used by R tests).

Zero-row REScalar round-trip (empty frame). @export

Parallel REScalar round-trip. @export

Column-expansion round-trip. Columns: \verb{_type}, \code{id}, \code{c_1..c_2}, \code{s_1..s_3}.

Produce the writer output for a mixed REExpand frame. @export

Parallel REExpand round-trip. @export

as_factor nested enum round-trip. Columns: \verb{_type}, \code{id}, \code{dir} (factor).

Produce the writer output for a mixed REMove frame. @export

Nested payload-bearing enum flatten round-trip. Columns: \verb{_type}, \code{id}, \code{status_variant}, \code{status_code}.

Produce the writer output for a mixed RETracked frame. @export

Parallel RETracked round-trip (delegates to sequential due to Struct field). @export

Struct-flatten variant field round-trip. Columns: \verb{_type}, \code{id}, \code{p_x}, \code{p_y}.

Produce the writer output for a mixed RELoc frame. @export

Parallel RELoc round-trip (delegates to sequential due to Struct field). @export

BTreeMap map-column round-trip. Columns: \verb{_type}, \code{label}, \code{tally_keys}, \code{tally_values}.

Produce the writer output for a mixed REMapB frame. Includes an empty-map row (\code{tally_keys}/\code{tally_values} are \code{character(0)}/\code{integer(0)}) and an absent-variant row (both list cells NULL). @export

Parallel REMapB round-trip. @export

HashMap map-column round-trip, asserted internally.

Builds a mixed \code{REMapH} frame (payload + empty-map + absent-variant rows), writes it, reads it back with the reader, and compares the reconstructed rows to the originals. \code{HashMap} equality is order-independent, so this proves the \verb{_keys} / \verb{_values} list-columns regroup back into the correct key→value associations regardless of iteration order. Returns \code{TRUE} on success. @export
}
