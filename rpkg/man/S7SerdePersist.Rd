% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/miniextendr-wrappers.R
\name{S7SerdePersist}
\alias{S7SerdePersist}
\alias{S7SerdePersist-s7_persist_label}
\alias{S7SerdePersist$s7_persist_label}
\alias{S7SerdePersist_s7_persist_label}
\alias{S7SerdePersist-s7_persist_values}
\alias{S7SerdePersist$s7_persist_values}
\alias{S7SerdePersist_s7_persist_values}
\alias{S7SerdePersist-s7_persist_note}
\alias{S7SerdePersist$s7_persist_note}
\alias{S7SerdePersist_s7_persist_note}
\alias{S7SerdePersist-s7_persist_id}
\alias{S7SerdePersist$s7_persist_id}
\alias{S7SerdePersist_s7_persist_id}
\alias{S7SerdePersist-s7_persist_lookup_get}
\alias{S7SerdePersist$s7_persist_lookup_get}
\alias{S7SerdePersist_s7_persist_lookup_get}
\alias{S7SerdePersist-s7_persist_to_r}
\alias{S7SerdePersist$s7_persist_to_r}
\alias{S7SerdePersist_s7_persist_to_r}
\alias{S7SerdePersist_from_r_data}
\title{S7SerdePersist S7 Class}
\source{
Generated by miniextendr from Rust type \code{S7SerdePersist}

Generated by miniextendr from \code{S7SerdePersist::s7_persist_label}

Generated by miniextendr from \code{S7SerdePersist::s7_persist_label}

Generated by miniextendr from \code{S7SerdePersist::s7_persist_values}

Generated by miniextendr from \code{S7SerdePersist::s7_persist_values}

Generated by miniextendr from \code{S7SerdePersist::s7_persist_note}

Generated by miniextendr from \code{S7SerdePersist::s7_persist_note}

Generated by miniextendr from \code{S7SerdePersist::s7_persist_id}

Generated by miniextendr from \code{S7SerdePersist::s7_persist_id}

Generated by miniextendr from \code{S7SerdePersist::s7_persist_lookup_get}

Generated by miniextendr from \code{S7SerdePersist::s7_persist_lookup_get}

Generated by miniextendr from \code{S7SerdePersist::s7_persist_to_r}

Generated by miniextendr from \code{S7SerdePersist::s7_persist_to_r}

Generated by miniextendr from \code{S7SerdePersist::from_r_data}
}
\usage{
S7SerdePersist(label, values, maybe_note, keys, counts, .ptr = NULL)

S7SerdePersist_s7_persist_label(self, ...)

S7SerdePersist_s7_persist_values(self, ...)

S7SerdePersist_s7_persist_note(self, ...)

S7SerdePersist_s7_persist_id(self, ...)

S7SerdePersist_s7_persist_lookup_get(self, key, ...)

S7SerdePersist_s7_persist_to_r(self, ...)

S7SerdePersist_from_r_data(data)
}
\arguments{
\item{label}{Character scalar label.}

\item{values}{Numeric vector payload.}

\item{maybe_note}{Optional character note (NULL for none).}

\item{keys}{Character vector of lookup keys.}

\item{counts}{Integer vector of lookup values (parallel to keys).}

\item{.ptr}{Internal pointer (used by static methods, not for direct use).}

\item{self}{A \code{S7SerdePersist} object.}

\item{...}{Additional arguments; ignored by the fast-path shortcut.}

\item{key}{(undocumented)}

\item{data}{Named list produced by \code{s7_persist_to_r()}.}
}
\description{
S7 class for cross-session serde persistence tests.

Returns the label.

Calls the underlying Rust routine directly, bypassing \code{S7::S7_dispatch()}
(the class walk + method-table lookup). Use in hot loops where the
per-call dispatch overhead matters. \strong{Footgun:} this shortcut does not
perform subclass dispatch — a method override defined on a child class
will \emph{not} be honoured. Use the generic when subclassing is possible.

Returns the numeric payload.

Calls the underlying Rust routine directly, bypassing \code{S7::S7_dispatch()}
(the class walk + method-table lookup). Use in hot loops where the
per-call dispatch overhead matters. \strong{Footgun:} this shortcut does not
perform subclass dispatch — a method override defined on a child class
will \emph{not} be honoured. Use the generic when subclassing is possible.

Returns the optional note. Note: as a class METHOD, \code{Option::None} raises the typed NONE_ERR condition instead of returning NA — the method path diverges from the standalone-fn absence contract (#1415).

Calls the underlying Rust routine directly, bypassing \code{S7::S7_dispatch()}
(the class walk + method-table lookup). Use in hot loops where the
per-call dispatch overhead matters. \strong{Footgun:} this shortcut does not
perform subclass dispatch — a method override defined on a child class
will \emph{not} be honoured. Use the generic when subclassing is possible.

Returns the u64 id formatted as a string (value exceeds R's i32).

Calls the underlying Rust routine directly, bypassing \code{S7::S7_dispatch()}
(the class walk + method-table lookup). Use in hot loops where the
per-call dispatch overhead matters. \strong{Footgun:} this shortcut does not
perform subclass dispatch — a method override defined on a child class
will \emph{not} be honoured. Use the generic when subclassing is possible.

Looks up a key in the Rust-side map. A missing key raises the typed NONE_ERR condition ("returned no value") rather than returning NA — the method path diverges from the standalone-fn absence contract (#1415); the cross-session test pins this behavior.

Calls the underlying Rust routine directly, bypassing \code{S7::S7_dispatch()}
(the class walk + method-table lookup). Use in hot loops where the
per-call dispatch overhead matters. \strong{Footgun:} this shortcut does not
perform subclass dispatch — a method override defined on a child class
will \emph{not} be honoured. Use the generic when subclassing is possible.

Lowers the full Rust state to plain R data (a named list) via serde — safe to \code{saveRDS}/\code{save} and reload in another session.

Calls the underlying Rust routine directly, bypassing \code{S7::S7_dispatch()}
(the class walk + method-table lookup). Use in hot loops where the
per-call dispatch overhead matters. \strong{Footgun:} this shortcut does not
perform subclass dispatch — a method override defined on a child class
will \emph{not} be honoured. Use the generic when subclassing is possible.

Rebuilds a live object from serde-lowered R data; wraps into a classed S7 object exactly like the constructor does.
}
\keyword{internal}
